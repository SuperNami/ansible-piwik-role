---
# Root MySQL user
mysql_root_user: "{{ vault_mysql_root_user }}"
mysql_root_password: "{{ vault_mysql_root_password }}"

# Piwik
piwik_db_user: "{{ vault_db_user }}"
piwik_db_user_password: "{{ vault_db_user_password }}"
